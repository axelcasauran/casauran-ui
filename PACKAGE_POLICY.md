# Package Policy
Supported packages: react/tokens/theme/icons. Other workspace packages are internal implementation modules.

Every package has explicit exports, no undeclared imports, correct CSS sideEffects, no cycles and clear ownership. React is a peer where it is part of the supported integration contract. Internal modularity does not automatically create public API obligations.
