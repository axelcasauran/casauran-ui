# Versioning

Semantic versioning governs supported public packages: patch for compatible fixes, minor for compatible features/components, major for supported breaking changes.

Deprecation should precede removal when feasible. Experimental APIs are outside stable guarantees only when clearly marked. Internal package versions may move together to reduce transitive compatibility risk.
