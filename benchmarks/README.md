# Benchmarks

Add benchmark suites only when a stage defines a meaningful performance scenario. Record environment, dataset, interaction path, metric and accepted variance. Avoid vanity microbenchmarks.
