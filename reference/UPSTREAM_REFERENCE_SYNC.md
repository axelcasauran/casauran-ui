# Upstream Reference Sync

Use `.agent/workflows/reference-sync.md`. The baseline is immutable during ordinary work. A newer upstream commit is diffed, classified and explicitly approved before changing scope/provenance.
