# Integration Architecture
Integrate through public state/event/data contracts rather than hard-coded providers.

Potential integrations include server data, cloud upload/storage, export backends, AI providers, form libraries and alternate data engines. They remain optional adapters/examples unless explicitly promoted. Base components remain usable without provider accounts or SDKs.
