# Adapter Architecture
Adapters are optional, domain-owned and evidence-driven. A contract is introduced only at a genuine replacement boundary. Native implementation is default. External adapter arrives only after dependency approval and a real alternate implementation/integration.

No generic adapter registry. Positioning adapters belong to positioning; data adapters to data; date adapters to date-math. Public types remain adapter-independent.
