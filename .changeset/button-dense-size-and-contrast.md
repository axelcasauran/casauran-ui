---
'@casauran/react': minor
---

Button gains a dense `xs` control size, keeps icon-only actions square at every size, and paints
readable text for the `outline`, `ghost` and `link` appearances on the neutral tone. The size
addition is additive; the geometry and colour corrections change rendered CSS only and require no
consumer change.
