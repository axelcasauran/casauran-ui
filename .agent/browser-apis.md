# Browser Apis

Preferred APIs: getBoundingClientRect, ResizeObserver, IntersectionObserver, MutationObserver, Pointer Events, CSS/WAAPI, requestAnimationFrame, Constraint Validation, FormData, Clipboard, Intl, URL, AbortController, matchMedia, logical CSS. Every use considers SSR guards/support/cleanup.
