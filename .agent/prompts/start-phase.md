# Start Phase Prompt

Prepare Phase `{{PHASE}}` without public implementation. Verify prerequisite phase certification, green validators/CI, registry/stage entries, pinned baseline, prerequisite engines/components, unresolved ADRs and invalidating debt. Return exact stage order and blocking risks. Reorder only with documented dependency evidence.
