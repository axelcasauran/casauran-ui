# Foundation Stage Prompt
Execute only `{{STAGE_ID}}`. Foundation work may create shared packages/schemas/validators/platform infrastructure but no future public components. Define contract/ownership/tests, validate, update evidence, stop at stage boundary.
