# Component DNA

Created only when a public component stage starts:

`packages/react/src/components/<domain>/<slug>/`

Completed source normally contains:

- `index.ts`
- implementation/types
- component CSS
- component tokens
- tests
- deterministic example/story modules
- generated metadata pointer where useful

Canonical spec and registry remain at repository root; do not maintain duplicate hand-edited manifests.
