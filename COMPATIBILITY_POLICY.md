# Compatibility Policy

`COMPATIBILITY.md` defines dimensions; this policy defines governance.

Support ranges are tested and documented. Dropping a supported React/Next/browser/Node environment is a release decision with migration notes. Adding support requires proof through CI/integration scenarios rather than optimistic version ranges.
