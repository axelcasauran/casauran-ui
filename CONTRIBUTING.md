# Contributing

Work only in the active stage/phase. Read governing docs and skills. Keep one public component per component stage. Specs/registry/tests precede completion. Run `pnpm validate`. Add release metadata when supported behavior changes. Update stage evidence.

Architectural proposals use ADR workflow rather than opportunistic refactors.
