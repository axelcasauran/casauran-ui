# showcase

Next.js App Router host on port 3102. Purpose: application-level product showcase.

Routes are Server Components by default; client components are introduced only where interaction requires them.
